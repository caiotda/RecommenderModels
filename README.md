# bpr-mf
Pytorch based implementation of Matrix factorization using Bayesian Personalized Ranking


### Setup

First, create a virtual enviromnent using conda:

```
conda env create -f environment.yml
conda activate <env_name>
```


run `pip install -e <repo_name>`